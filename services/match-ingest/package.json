{
  "name": "@platzifc/match-ingest",
  "version": "0.0.1",
  "private": true,
  "description": "Match data ingestion service — Vercel Edge Function / Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@platzifc/config": "workspace:*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@platzifc/eslint-config": "workspace:*",
    "@platzifc/ts-config": "workspace:*",
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.65.0"
  }
}
